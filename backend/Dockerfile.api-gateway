# See Dockerfile.auth-service for why distroless/static over alpine.
FROM golang:1.27-alpine AS build
WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -o /out/api-gateway ./cmd/api-gateway

FROM gcr.io/distroless/static:nonroot
COPY --from=build /out/api-gateway /api-gateway
USER nonroot:nonroot

# GraphQL HTTP port — see backend/.env.example.
EXPOSE 8080

ENTRYPOINT ["/api-gateway"]
