# Illustrative shape only — see k8s/base/auth-service/secret.example.yaml
# for the full explanation of this convention.
apiVersion: v1
kind: Secret
metadata:
  name: users-service-secrets
  namespace: skill-bridge
type: Opaque
stringData:
  DATABASE_URL: "postgres://users_service:CHANGE_ME@postgres.skill-bridge.svc.cluster.local:5432/postgres"
