apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: skill-bridge
  labels:
    app: users-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
        - name: users-service
          image: skill-bridge/users-service:kind
          imagePullPolicy: Never
          ports:
            - name: grpc
              containerPort: 9003
            - name: http
              containerPort: 8083
          envFrom:
            - configMapRef:
                name: users-service-config
            - secretRef:
                name: users-service-secrets
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8083
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
