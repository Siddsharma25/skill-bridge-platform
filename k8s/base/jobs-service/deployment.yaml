apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobs-service
  namespace: skill-bridge
  labels:
    app: jobs-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jobs-service
  template:
    metadata:
      labels:
        app: jobs-service
    spec:
      containers:
        - name: jobs-service
          image: skill-bridge/jobs-service:kind
          imagePullPolicy: Never
          ports:
            - name: grpc
              containerPort: 9004
            - name: http
              containerPort: 8084
          envFrom:
            - configMapRef:
                name: jobs-service-config
            - secretRef:
                name: jobs-service-secrets
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8084
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 10
